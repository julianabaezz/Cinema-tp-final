// import { usersApi } from "../../api";
import { AddUserType } from "../../types/models";

const useUsers = () =>{
    const addUser= (datos: AddUserType) =>{

        
    };
    return {addUser}
}

export {useUsers}